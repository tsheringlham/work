let names = ['sonam','zangmo','lham'];

 names[3] = 'wangmo';
// names.push('soyang');       //one way * 
// names.unshift('zam');        // push * add at
// names.pop();                // add at the first
               // will remove last array, 
console.log(names[2]); // will show the words
console.log(names.length); // it will show the number 


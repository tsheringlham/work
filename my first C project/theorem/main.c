#include <stdio.h>
#include <stdbool.h>
#include <string.h>

int main()
{
    int n,m;
    int a,b;
    printf("if a is even and b is even, then a+b is even\n\n ");

    printf("enter value for n:");
    scanf("%d", &n);
    printf("enter value for m:");
    scanf("%d", &m);

   int ab = (a*n)+(b*m);
   printf("is even number");







    return 0;
}
